---
title: 'API Reference'
description: 'REST endpoints for Hyper Leverage'
---

# API Reference

Base URL: `https://api.hyperleverage.app`

## Authentication

All endpoints require Bearer token authentication:

```bash
Authorization: Bearer YOUR_API_KEY
```

Get your API key from the dashboard after beta signup.

## Rate Limits

- **Public endpoints:** 100 req/min  
- **Authenticated:** 500 req/min  
- **WebSocket:** 10 connections per IP

Exceeding limits returns `429 Too Many Requests`.

## Response Format

All responses follow this structure:

```json
{
  "success": true,
  "data": { ... },
  "timestamp": 1234567890
}
```

Errors return:

```json
{
  "success": false,
  "error": "Error message",
  "code": "ERROR_CODE"
}
```

## Endpoints

Browse the endpoints in the sidebar to see request/response schemas.
